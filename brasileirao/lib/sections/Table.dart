import 'package:flutter/material.dart';

class TabelaWidget extends StatelessWidget {
  final List jsonObjects;
  const TabelaWidget({super.key, this.jsonObjects = const[]});

  @override
  Widget build(BuildContext context) {
    return ListView(
      
    )
  }
}
